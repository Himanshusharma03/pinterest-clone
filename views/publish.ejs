<% include ./partials/header.ejs %>
  <% include ./partials/nav.ejs %>

    <section class="max-w-6xl w-full mx-auto mt-8 bg-white p-6 rounded-xl border border-gray-200 shadow">
      <form action="/createpost" method="POST" enctype="multipart/form-data" class="flex flex-col items-center gap-4">

        <!-- File Upload Input -->
        <label for="file-upload"
          class="flex flex-col items-center justify-center w-full md:w-2/4 h-40 bg-gray-50 border-2 border-dashed border-gray-300 rounded-xl cursor-pointer hover:bg-red-50 hover:border-red-700 transition">
          <div class="text-red-700 text-2xl mb-1">📌</div>
          <p class="text-sm text-gray-600">Click or drag image here to upload</p>
          <input type="file" id="file-upload" name="file" accept="image/*" required hidden>
        </label>

        <!-- Optional Caption Input -->
        <input type="text" name="filecaption" placeholder="Add a caption (optional)"
          class="w-full md:w-2/4 px-4 py-2 rounded-lg border border-gray-300 bg-gray-100 text-sm placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 h-12">

        <!-- Submit Button -->
        <input type="submit" value="Publish"
          class="bg-red-700 text-white font-bold text-sm px-6 py-2 rounded-full hover:bg-red-800 transition duration-200 cursor-pointer">

      </form>
    </section>

    <% include ./partials/footer.ejs %>